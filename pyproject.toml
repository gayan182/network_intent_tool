[project]
name = "network-intent-tool"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "netmiko>=4.7.0",
    "pyyaml>=6.0.3",
]
